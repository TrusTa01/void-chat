name: void_chat
description: "A messanger/chat"
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  # localization
  flutter_localizations:
    sdk: flutter
  flutter_localized_locales: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  # localization
  intl: ^0.20.2

  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true
  generate: true
